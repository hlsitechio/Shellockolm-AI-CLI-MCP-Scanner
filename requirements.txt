# Shellockolm runtime dependencies.
# These mirror [project.dependencies] in pyproject.toml — keep them in sync.
# Dev/test deps (pytest, black, flake8, mypy, ...) live in pyproject's
# [project.optional-dependencies] dev and are installed with: pip install .[dev]
typer[all]>=0.9.0
rich>=13.0.0
prompt_toolkit>=3.0.0
mcp>=1.0.0
pydantic>=2.0.0
packaging>=23.0
aiofiles>=23.0.0
requests>=2.28.0
